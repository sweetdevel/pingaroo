{% extends "layouts/app.phtml" %} 

{% block header_title %}Pingaroo{% endblock %}
{% block body_title %}View Proxy{% endblock %}
 
{% block content %}
<div class="row">
    <div class="col-xs-4">
        <!-- /.box-header -->
        <div class="box-body table-responsive no-padding">
            <table class="table table-hover">
                <tbody>
                    <tr>
                        <td> ID </td>
                        <td><input type="text" class="form-control"
                               name="id" id="id" value="<?php echo $proxy->id; ?>" 
                               disabled="disabled" />
                        </td>
                    </tr>
                    <tr>
                        <td> Country </td>
                        <td><input type="text" class="form-control"
                               name="id" id="id" value="<?php
                                    $country = $proxy->getCountry();
                                    echo $country->id . ' - ' . $country->name; 
                               ?>"
                               disabled="disabled" />
                        </td>
                    </tr>
                    <tr>
                        <td> Address </td>
                        <td><input type="text" class="form-control"
                               name="id" id="id" value="<?php echo $proxy->address; ?>"
                               disabled="disabled" />
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
</div>

<a href="{{ url('proxy') }}">
   <button type="button" class="btn btn-sm btn-default">Back</button>
</a>

{{ partial('common/partials/confirmModelDelete') }}
<?php 
    echo '<button type="button" class="btn btn-sm btn-danger" onclick="confirmModelDelete(' . $proxy->id . ')">Delete</button>'
    .Phalcon\Tag::form(array('proxy/delete/' . $proxy->id, 'method' => 'post', 'id'=>'modelForm_' . $proxy->id));
?>
{% endblock %}

{% block bottom %}
{% endblock %}